<ul class="nav nav-pills flex-column">
    <li class="nav-item"><a class="nav-link @(ViewBag.Code == "P" ? "active" : "")" id="profile" href="/Account/Manage">Profile</a></li>
    <li class="nav-item"><a class="nav-link @(ViewBag.Code == "PN" ? "active" : "")" id="phone-numbers" href="/Account/Manage/PhoneNumbers">Phone Numbers</a></li>
    <li class="nav-item"><a class="nav-link @(ViewBag.Code == "E" ? "active" : "")" id="email" href="/Account/Manage/Emails">Emails</a></li>
    @*<li class="nav-item"><a class="nav-link @(ViewBag.Code == "PA" ? "active" : "")" id="change-password" href="/Identity/Account/Manage/ChangePassword">Password</a></li>*@
    <li class="nav-item"><a class="nav-link @(ViewBag.Code == "D" ? "active" : "")" id="personal-data" href="/Account/Manage/PersonalData">Personal data</a></li>
    @if (User.IsInRole("Regimental"))
    { 
        <li class="nav-item">
            <a class="nav-link @(ViewBag.Code == "R" ? "active" : "")" id="regimental-details" href="/Account/Manage/Regimental">Regimental</a>
        </li>
    }
    @if (User.IsInRole("Volunteer"))
    { 
        <li class="nav-item">
            <a class="nav-link @(ViewBag.Code == "V" ? "active" : "")" id="volunteer-details" href="/Account/Manage/Volunteer">Volunteer</a>
        </li>
    }
    @if (User.IsInRole("Staff"))
    { 
        <li class="nav-item">
            <a class="nav-link @(ViewBag.Code == "S" ? "active" : "")" id="staff-details" href="/Account/Manage/Staff">Staff</a>
        </li>
    }
    @if (User.IsInRole("Member"))
    { 
        <li class="nav-item">
            <a class="nav-link @(ViewBag.Code == "M" ? "active" : "")" id="member-details" href="/Account/Manage/Member">Member</a>
        </li>
    }
</ul>